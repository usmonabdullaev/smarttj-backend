/**
 * Приоритет отправки сообщения в очереди SMSGate API.
 * Более высокий приоритет обрабатывается быстрее.
 * 0: Low (Низкий приоритет)
 * 1: Normal (Обычный приоритет по умолчанию)
 * 2: High (Высокий приоритет для OTP / 2FA)
 */
export enum SmsPriority {
  LOW = 0,
  NORMAL = 1,
  HIGH = 2,
}
